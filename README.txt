Usage:
    sudo pip install requirements.txt
    chmod +x eigentrust.py
    ./eigentrust.py --help
    ./eigentrust.py --scenario one --nodes 50 --repl

Assumes familiarity with Python (http://python.org/).
